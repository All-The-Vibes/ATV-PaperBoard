{
  "hooks": {
    "PostToolUse": [{
      "matcher": "Write",
      "hooks": [{
        "type": "command",
        "command": "python ${CLAUDE_PLUGIN_ROOT}/core/cli.py detect-artifact-candidate \"${TOOL_OUTPUT}\"",
        "timeout": 2
      }]
    }]
  }
}
